# TCTI Student Portal API

Production-grade API for Tashkent University of Information Technologies student portal with local network access.

## Features
- 🔍 DOM structure analysis endpoint (`/api/login-structure`)
- 🔑 Secure authentication endpoint (`/api/login`)
- 🌐 Automatic local network discovery
- 🛡️ Security headers and rate limiting
- 📱 Wi-Fi accessible API

## Setup
```bash
# Install dependencies
npm install

# Create .env file
cp .env.example .env